import { createTheme } from "@material-ui/core/styles";

const arcBlue = "#22599c";
const arcGrey = "#868686";

export default createTheme({
  palette: {
    primary: {
      main: arcBlue,
    },
    secondary: {
      main: arcGrey,
    },
  },
});
